import React from "react"
import {Paper, Typography} from "@material-ui/core"

const SideTab = () => (
    <Paper variant="outlined">
        <Typography variant="h1">side bar</Typography>
    </Paper>
);

export default SideTab;